{
  "name": "pcbgenius-backend",
  "version": "0.1.0",
  "private": true,
  "description": "PCBGenius V1 Core — Cloudflare Workers gateway exposing the 10 frozen contract tool-call endpoints as stubs.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^3.60.0"
  }
}
